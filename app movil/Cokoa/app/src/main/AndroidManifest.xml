<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.cokoa">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.LogrosActivity"
            android:label="Logros"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Activities.NotasActivity"
            android:label="Nota Actividad"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Activities.InasistenciaMateriaActivity"
            android:label="Inasistencia"
            android:theme="@style/AppTheme" />
        <activity android:name=".ActivityProfesor.EstudiantesCursoActivity" />
        <activity
            android:name=".ActivityProfesor.LogrosMateriaEstudianteProfesorActivity"
            android:label="Logros"/>
        <activity android:name=".ActivityProfesor.LlamarListaActivity" />
        <activity android:name=".ActivityProfesor.InsertarNotaActividadActivity"></activity>

        <service
            android:name=".MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service
            android:name=".MyFirebaseInstanceIdService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>